{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "dev": {
      "persistent": true
    },
    "build": {
      "outputs": ["dist/**", ".wrangler/deploy/**"]
    },
    "check:types": {
      "outputs": [".source/**"]
    }
  }
}
