# Mitome documentation

```sh
bun run dev
bun run check:types
bun run check:links
bun run test
bun run build
```

`check:types` compiles included TypeScript examples and runs source-level internal link and anchor validation. The production build retains TanStack Start's prerender crawl as a backstop.
