{
  "name": "@mitome/core",
  "version": "0.0.0",
  "private": true,
  "files": [
    "dist"
  ],
  "type": "module",
  "sideEffects": false,
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "rm -rf dist && tsc",
    "check:types": "tsc --noEmit && tsc -p tsconfig.types.json",
    "test": "bun test test"
  },
  "dependencies": {
    "effect": "catalog:"
  },
  "devDependencies": {
    "@mitome/config": "workspace:*",
    "@types/bun": "catalog:",
    "typescript": "catalog:"
  }
}
