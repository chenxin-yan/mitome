# @mitome/sdk

Mitome's public authoring API.

```sh
npm install @mitome/sdk @mitome/providers
```

Use `@mitome/sdk` for Promise-first APIs or `@mitome/sdk/effect` for the complete Effect-native API. Their `TurnEvent` types match their respective runtimes.
